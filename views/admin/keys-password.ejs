<div class="keys-password-page">
  <div class="password-wrapper">
    <div class="password-container">
      <div class="password-box">
        <div class="password-header">
          <div class="lock-icon">🔒</div>
          <h1>Xác thực mật khẩu</h1>
          <p class="password-subtitle">Vui lòng nhập mật khẩu để truy cập quản lý Key</p>
        </div>
        
        <% if (error) { %>
          <div class="error-message">
            <span class="error-icon">❌</span>
            <span>Mật khẩu không đúng. Vui lòng thử lại.</span>
          </div>
        <% } %>
        
        <form method="POST" action="/admin/keys/verify" class="password-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="form-group">
            <label for="password">Mật khẩu</label>
            <input
              type="password"
              id="password"
              name="password"
              required
              autofocus
              class="password-input"
              placeholder="Nhập mật khẩu..."
            />
          </div>
          
          <button type="submit" class="btn-submit">Xác nhận</button>
        </form>
        
        <div class="password-footer">
          <a href="/admin" class="btn-back">← Quay lại Admin</a>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.keys-password-page {
  min-height: calc(100vh - 200px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
  position: relative;
  z-index: 1;
}

.password-wrapper {
  width: 100%;
  max-width: 500px;
  margin: 0 auto;
}

.password-container {
  width: 100%;
}

.password-box {
  width: 100%;
  background: var(--card);
  border: 2px solid var(--border);
  border-radius: 20px;
  padding: 50px 40px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
  position: relative;
  z-index: 10;
}

.password-header {
  text-align: center;
  margin-bottom: 40px;
}

.lock-icon {
  font-size: 64px;
  margin-bottom: 20px;
  display: block;
}

.password-header h1 {
  margin: 0 0 12px 0;
  font-size: 32px;
  font-weight: 700;
  color: var(--fg);
}

.password-subtitle {
  margin: 0;
  color: var(--muted);
  font-size: 15px;
  line-height: 1.5;
}

.error-message {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 16px 20px;
  background: rgba(239, 68, 68, 0.15);
  border: 2px solid rgba(239, 68, 68, 0.4);
  border-radius: 10px;
  color: #ef4444;
  margin-bottom: 30px;
  font-size: 15px;
  font-weight: 500;
}

.error-icon {
  font-size: 20px;
  flex-shrink: 0;
}

.password-form {
  margin-bottom: 30px;
}

.form-group {
  margin-bottom: 24px;
}

.form-group label {
  display: block;
  margin-bottom: 10px;
  color: var(--fg);
  font-weight: 600;
  font-size: 15px;
}

.password-input {
  width: 100%;
  padding: 16px 20px;
  border: 2px solid var(--border);
  border-radius: 12px;
  background: var(--bg);
  color: var(--fg);
  font-size: 16px;
  font-family: inherit;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

.password-input:focus {
  outline: none;
  border-color: var(--green);
  box-shadow: 0 0 0 4px rgba(22, 163, 74, 0.15);
  background: var(--card);
}

.btn-submit {
  width: 100%;
  padding: 16px 24px;
  border: none;
  border-radius: 12px;
  font-size: 17px;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  background: var(--green);
  color: white;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.btn-submit:hover {
  background: #16a34a;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(22, 163, 74, 0.4);
}

.btn-submit:active {
  transform: translateY(0);
}

.password-footer {
  text-align: center;
  padding-top: 24px;
  border-top: 2px solid var(--border);
  margin-top: 24px;
}

.btn-back {
  display: inline-block;
  padding: 12px 24px;
  background: transparent;
  border: 2px solid var(--border);
  border-radius: 10px;
  color: var(--fg);
  text-decoration: none;
  font-size: 15px;
  font-weight: 600;
  transition: all 0.3s ease;
}

.btn-back:hover {
  background: rgba(255, 255, 255, 0.05);
  border-color: var(--green);
  color: var(--green);
  transform: translateX(-4px);
}

/* Responsive */
@media (max-width: 768px) {
  .keys-password-page {
    padding: 20px 15px;
    min-height: calc(100vh - 150px);
  }
  
  .password-box {
    padding: 40px 30px;
    border-radius: 16px;
  }
  
  .lock-icon {
    font-size: 48px;
    margin-bottom: 16px;
  }
  
  .password-header h1 {
    font-size: 26px;
  }
  
  .password-subtitle {
    font-size: 14px;
  }
}

/* Override any conflicting styles */
.keys-password-page header,
.keys-password-page footer {
  display: none !important;
}

.keys-password-page .container {
  max-width: 100% !important;
  padding: 0 !important;
}
</style>

