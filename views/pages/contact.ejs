<div class="static-page">
  <h1>📞 Liên hệ</h1>
  
  <div class="contact-grid">
    <div class="contact-info">
      <div class="info-card">
        <div class="info-icon">📧</div>
        <h3>Email</h3>
        <p>support@safekeys.local</p>
        <p class="muted">Phản hồi trong 24h</p>
      </div>
      
      <div class="info-card">
        <div class="info-icon">📱</div>
        <h3>Hotline</h3>
        <p>0123 456 789</p>
        <p class="muted">8:00 - 22:00 hàng ngày</p>
      </div>
      
      <div class="info-card">
        <div class="info-icon">💬</div>
        <h3>Hỗ trợ trực tuyến</h3>
        <p>24/7</p>
        <p class="muted">Chat trực tiếp trên website</p>
      </div>
    </div>
    
    <div class="contact-form-card">
      <% if (typeof html !== 'undefined' && html && html.trim().length) { %>
        <div><%- html %></div>
      <% } else { %>
        <h2>Gửi tin nhắn cho chúng tôi</h2>
        <form action="mailto:support@safekeys.local" method="post" enctype="text/plain" class="contact-form">
          <div class="form-group">
            <label>Họ tên *</label>
            <input name="name" placeholder="Nhập họ tên" required />
          </div>
          
          <div class="form-group">
            <label>Email *</label>
            <input name="email" type="email" placeholder="your@email.com" required />
          </div>
          
          <div class="form-group">
            <label>Tiêu đề *</label>
            <input name="subject" placeholder="Chủ đề liên hệ" required />
          </div>
          
          <div class="form-group">
            <label>Nội dung *</label>
            <textarea name="body" rows="6" placeholder="Mô tả chi tiết vấn đề..." required></textarea>
          </div>
          
          <button type="submit" class="btn primary large">
            <span>📤</span>
            Gửi tin nhắn
          </button>
        </form>
      <% } %>
    </div>
  </div>
</div>

<style>
.static-page {
  max-width: 1000px;
  margin: 0 auto;
}

.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px;
  margin-top: 30px;
}

.contact-info {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.info-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
  text-align: center;
  transition: all 0.3s;
}

.info-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  border-color: var(--green);
}

.info-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.info-card h3 {
  margin: 0 0 8px 0;
  color: var(--fg);
}

.info-card p {
  margin: 4px 0;
  color: var(--fg);
  font-weight: 500;
}

.contact-form-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 30px;
}

.contact-form-card h2 {
  margin: 0 0 24px 0;
  color: var(--fg);
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 8px;
  color: var(--fg);
  font-weight: 500;
}

.form-group input,
.form-group textarea {
  width: 100%;
  padding: 12px;
  border: 1px solid var(--border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.02);
  color: var(--fg);
  font-family: inherit;
  font-size: 14px;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--green);
  background: rgba(255, 255, 255, 0.05);
}

@media (max-width: 768px) {
  .contact-grid {
    grid-template-columns: 1fr;
  }
}
</style>

